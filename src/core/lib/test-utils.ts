/**
 * Test utility to verify path alias resolution
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Test constant to verify path alias resolution
 */
export const GRIMOIRE_VERSION = '1.0.0';
